# Data Files

- `dev_jmp_blogs_archive.csv`: main occurrence-level CSV.
- `dev_jmp_blogs_semantic_long.csv`: one row per occurrence-classification value.
- `dev_jmp_blogs_taxonomy.csv`: classification labels and definitions.

The site also uses JSON files generated from the same public layer.
